import {GameStatusToSvgPaths} from './game-status-to-svg-paths.pipe';

describe('GameStatusToSvgPaths', () => {
  it('create an instance', () => {
    const pipe = new GameStatusToSvgPaths();
    expect(pipe).toBeTruthy();
  });
});
